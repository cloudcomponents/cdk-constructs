{
    "name": "@cloudcomponents/cdk-website-monitor-lambda",
    "version": "1.0.0",
    "private": true,
    "license": "MIT",
    "author": "hello@webappsolutions.de",
    "scripts": {
        "build": "rimraf lib && tsc",
        "watch": "rimraf lib && tsc -w"
    },
    "files": [
        "lib"
    ],
    "bundleDependencies": [
        "aws-sdk",
        "axios"
    ],
    "dependencies": {
        "aws-sdk": "^2.496.0",
        "axios": "^0.19.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.30",
        "rimraf": "^2.6.3",
        "typescript": "^3.5.3"
    }
}
